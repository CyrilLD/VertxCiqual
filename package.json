{
  "name": "ciqual-search",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "vertx3-full": "3.5.1"
  },
  "scripts": {
    "build": "rm -Rf dist && ./node_modules/.bin/babel --out-dir dist src",
    "start": "./node_modules/.bin/vertx run dist/index.js -conf conf/config.json"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
